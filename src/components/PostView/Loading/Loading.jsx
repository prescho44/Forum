/**
 * @module Loading
 * @description Simple loading indicator component
 * 
 * @component
 * 
 * @example
 * return (
 *   <Loading />
 * )
 * 
 * @returns {JSX.Element} Loading text element
 */

const Loading = () => {
  return <h1>Loading...</h1>;
};
export default Loading;
